import { ComponentProps, ComponentType } from 'react';
import type { VariableKey } from '@devcycle/types';
export declare const SwapComponents: <T extends ComponentType<any>>(OldComponent: T, NewComponent: T, variableKey: VariableKey) => (props: ComponentProps<T>) => React.ReactElement;
